version: '3.8'

services:
  proxy:
    build: .
    container_name: free-claude-proxy
    ports:
      - "8082:8082"
    env_file:
      - .env
    environment:
      - HOST=0.0.0.0
    restart: unless-stopped
